interface:
  display_name: "Strategy Scaffold"
  short_description: "Create or update strategy modules, registry entries, tests, and docs."
  default_prompt: "Use $strategy-scaffold to add a new trading strategy to this repository and wire it in correctly."

policy:
  allow_implicit_invocation: true
